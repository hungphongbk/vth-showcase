query ShowcaseDetail($slug: String!) {
  showcase(slug: $slug) {
    ...ShowcaseDetail
    ...ShowcaseHF
    ...ShowcaseInvestorStat
    updates{
      id
      content
      createdAt
    }
  }
  showcases(filter: { slug: { neq: $slug,notLike: "ci-test%" } }) {
    edges {
      node {
        slug
        name
        status
        image {
          ...Media
        }
        createdAt
      }
    }
  }
}