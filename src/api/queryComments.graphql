query QueryComments($slug: String!){
  showcase(slug: $slug){
    slug
    comments{
      nodes {
        id
        author {
          name
        }
        content
        rate
      }
    }
  }
}