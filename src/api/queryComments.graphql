query QueryComments($slug: String!){
  showcase(slug: $slug){
    slug
    comments{
      nodes {
        id
        author {
          email
          name
        }
        content
        rate
      }
    }
  }
}