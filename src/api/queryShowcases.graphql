query Showcases($paging: CursorPaging!) {
  showcases(paging: $paging)  {
    pageInfo{
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        name
        slug
        author
        status
        image {
          path
        }
        createdAt
      }
    }
  }
}