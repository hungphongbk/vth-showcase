query ShowcasePreview($slug: String!) {
  showcase(slug: $slug) {
    slug
    name
    author
    status
    image {
      path
    }
    description
    expectedSalePrice{
      regular
      pioneer
      preorder
      promo
    }
  }
  showcases(filter: { slug: { neq: $slug } }) {
    edges {
      node {
        slug
        name
        author
        status
        image {
          path
        }
        createdAt
      }
    }
  }
}

