query ShowcasePortal {
  showcases(paging:{first: 10})  {
    pageInfo{
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        name
        slug
        status
        image {
          path
          preloadUrl
          height
          width
        }
        createdAt
      }
    }
  }
}