import { StyledDialog } from "../commons";

type InvestorRegDialogProps = {};
export default function InvestorRegDialog(
  props: InvestorRegDialogProps
): JSX.Element {
  return <StyledDialog open={false}></StyledDialog>;
}
